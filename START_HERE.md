# 🎯 START HERE - Быстрый старт

## ✅ Что готово

Полнофункциональный сервис для отслеживания переходов по коротким ссылкам:

- ✅ **FastAPI** - асинхронный веб-сервер с редиректами
- ✅ **SQLite + aiosqlite** - база данных для ссылок и статистики
- ✅ **Typer CLI** - управление через командную строку
- ✅ **Telegram Bot** - автоматические отчеты
- ✅ **APScheduler** - планировщик отчетов
- ✅ **Docker + Docker Compose** - контейнеризация
- ✅ **Makefile** - удобные команды
- ✅ **Документация** - подробные инструкции

## 🚀 Запуск за 3 шага

### 1. Создайте .env файл

```bash
cat > .env << 'EOF'
HOST=0.0.0.0
PORT=8000
DATABASE_PATH=/app/data/links.db

TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=123456789

DAILY_REPORT_TIME=09:00
WEEKLY_REPORT_DAY=monday
WEEKLY_REPORT_TIME=09:00
TIMEZONE=Europe/Moscow

LOG_LEVEL=INFO
EOF
```

**Замените:**
- `TELEGRAM_BOT_TOKEN` - токен от @BotFather
- `TELEGRAM_CHAT_ID` - ваш ID от @userinfobot

### 2. Запустите сервисы

```bash
docker compose up -d
```

### 3. Проверьте работу

```bash
# Проверить здоровье
curl http://localhost:8000/health

# Создать тестовую ссылку
docker compose exec web uv run python -m src.cli create test https://google.com --title "Test Link"

# Посмотреть список
docker compose exec web uv run python -m src.cli list

# Протестировать редирект
curl -L http://localhost:8000/test
```

## 📋 Основные команды

### Через Makefile (рекомендуется)

```bash
# Управление
make up              # Запустить
make down            # Остановить
make restart         # Перезапустить
make logs            # Логи

# Работа со ссылками
make create CODE=doc1 URL=https://example.com TITLE="Doctor 1"
make list            # Список всех ссылок
make stats CODE=doc1 # Общая статистика
make clicks CODE=doc1 # Детали кликов (IP, браузер, время)
make delete CODE=doc1
make reset-clicks CODE=doc1  # Обнулить счетчик

# Отчеты
make report-daily    # Отправить дневной отчет
make report-weekly   # Отправить недельный отчет
```

### Через docker compose

```bash
# CLI команды
docker compose exec web uv run python -m src.cli create <code> <url> --title "Title"
docker compose exec web uv run python -m src.cli list
docker compose exec web uv run python -m src.cli stats <code>
docker compose exec web uv run python -m src.cli clicks <code> --limit 50
docker compose exec web uv run python -m src.cli update <code> <new-url>
docker compose exec web uv run python -m src.cli delete <code>
docker compose exec web uv run python -m src.cli reset-clicks <code>

# Отчеты
docker compose exec scheduler uv run python -m src.cli send-report daily
docker compose exec scheduler uv run python -m src.cli send-report weekly
```

## 📊 Архитектура

### Сервисы

1. **web** (port 8000) - FastAPI сервер для редиректов
2. **scheduler** - APScheduler для автоматических отчетов  
3. **init** - инициализация БД при старте

### Компоненты

- `src/main.py` - FastAPI приложение
- `src/cli.py` - CLI команды
- `src/database.py` - Работа с SQLite
- `src/telegram.py` - Telegram интеграция
- `src/scheduler.py` - Планировщик
- `src/config.py` - Конфигурация
- `src/models.py` - Pydantic модели

## 📖 Документация

- **README.md** - Полная документация (⭐️ читать первым!)
- **QUICKSTART.md** - Пошаговая инструкция
- **ENV_SETUP.md** - Настройка переменных окружения
- **DATA_PERSISTENCE.md** - Сохранность данных и backup
- **START_HERE.md** - Этот файл

## 🎯 Примеры использования

### Создать ссылки для врачей

```bash
make create CODE=ivanov URL=https://instagram.com/dr_ivanov TITLE="Доктор Иванов"
make create CODE=petrov URL=https://vk.com/dr_petrov TITLE="Доктор Петров"
make create CODE=sidorov URL=https://t.me/dr_sidorov TITLE="Доктор Сидоров"
```

### Посмотреть все ссылки

```bash
make list
```

Вывод:
```
┏━━━━━━━━━┳━━━━━━━━━━━━━━━━━━━━━━━━━━┳━━━━━━━━━━━━━━━━┳━━━━━━━━┳━━━━━━━━━━━━┓
┃ Code    ┃ URL                      ┃ Title          ┃ Clicks ┃ Created    ┃
┡━━━━━━━━━╇━━━━━━━━━━━━━━━━━━━━━━━━━━╇━━━━━━━━━━━━━━━━╇━━━━━━━━╇━━━━━━━━━━━━┩
│ ivanov  │ https://instagram.com... │ Доктор Иванов  │     12 │ 2025-11-06 │
│ petrov  │ https://vk.com/dr_petro..│ Доктор Петров  │      8 │ 2025-11-06 │
│ sidorov │ https://t.me/dr_sidorov  │ Доктор Сидоров │      5 │ 2025-11-06 │
└─────────┴──────────────────────────┴────────────────┴────────┴────────────┘
```

### Посмотреть статистику

#### Общая статистика

```bash
make stats CODE=ivanov DAYS=7
```

Вывод:
```
📊 Statistics: ivanov
   Title: Доктор Иванов

Last 7 days: 12 clicks
Total all time: 12 clicks
Average per day: 1.7 clicks
```

#### Детальная статистика кликов

```bash
make clicks CODE=ivanov LIMIT=20
```

Вывод:
```
🖱️  Recent clicks for ivanov:
Showing last 20 clicks

┏━━━━━━━━━━━━━━━━━━━━━┳━━━━━━━━━━━━━━━┳━━━━━━━━━━━━━━━━━━━━━━┳━━━━━━━━━━━━━━━┓
┃ Time                ┃ IP Address    ┃ User Agent           ┃ Referer       ┃
┡━━━━━━━━━━━━━━━━━━━━━╇━━━━━━━━━━━━━━━╇━━━━━━━━━━━━━━━━━━━━━━╇━━━━━━━━━━━━━━━┩
│ 2025-11-07 14:23:15 │ 192.168.1.10  │ Mozilla/5.0 Chrome/… │ https://vk.…  │
│ 2025-11-07 13:45:22 │ 192.168.1.15  │ Safari/17.0 iPhone…  │ direct        │
└─────────────────────┴───────────────┴──────────────────────┴───────────────┘
```

### Отправить отчет в Telegram

```bash
make report-daily
```

Telegram отчет:
```
📊 Статистика за 24 часа
📅 06.11.2025
━━━━━━━━━━━━━━━━━━━━

👨‍⚕️ Доктор Иванов
├─ Сегодня: 12 👆
├─ Всего: 12
└─

👨‍⚕️ Доктор Петров
├─ Сегодня: 8 👆
├─ Всего: 8
└─

━━━━━━━━━━━━━━━━━━━━
Всего: 20 переходов
```

## 🔍 Проверка работы

### Healthcheck

```bash
curl http://localhost:8000/health
```

Ответ:
```json
{
  "status": "healthy",
  "service": "link-tracker",
  "version": "0.1.0"
}
```

### Тестовый редирект

```bash
# Создать ссылку
make create CODE=test URL=https://google.com

# Протестировать (откроется Google)
curl -L http://localhost:8000/test
```

### API статистика

```bash
curl http://localhost:8000/api/links/test/stats | jq
```

## 🛠️ Troubleshooting

### Сервис не запускается?

```bash
# Проверьте логи
make logs

# Пересоберите
make rebuild
```

### Telegram не работает?

1. Проверьте токен и chat_id в `.env`
2. Отправьте тестовый отчет: `make report-daily`
3. Проверьте логи scheduler: `make logs-scheduler`

### База данных заблокирована?

```bash
make restart
```

## 📦 Структура проекта

```
url_shortener/
├── src/                    # Исходный код
│   ├── main.py            # FastAPI сервер
│   ├── cli.py             # CLI команды
│   ├── database.py        # БД операции
│   ├── telegram.py        # Telegram бот
│   ├── scheduler.py       # Планировщик
│   ├── models.py          # Pydantic модели
│   └── config.py          # Конфигурация
├── docker/
│   └── Dockerfile         # Docker образ
├── data/                  # База данных (volume)
│   └── links.db          # SQLite БД
├── docker-compose.yml     # Docker Compose
├── Makefile              # Удобные команды
├── .env                  # Конфигурация (создать!)
└── README.md             # Документация
```

## 🎓 Следующие шаги

1. ✅ Запустить сервисы: `docker compose up -d`
2. ✅ Создать первую ссылку
3. ✅ Протестировать редирект
4. ✅ Проверить отчеты в Telegram
5. 📖 Прочитать полную документацию в **README.md**

## 💡 Полезные ссылки

- Web UI: http://localhost:8000
- Health: http://localhost:8000/health
- API Stats: http://localhost:8000/api/links/{code}/stats

## 🤝 Помощь

Если что-то не работает:

1. 📖 Прочитайте [README.md](README.md#-troubleshooting)
2. 📊 Проверьте логи: `make logs`
3. 🔄 Перезапустите: `make restart`
4. 🏗️ Пересоберите: `make rebuild`

---

**Готово к продакшену! 🚀**

Все работает через Docker - просто `docker compose up -d` и сервис готов!


