# 🚑 Исправление статистики (Timezone + Format)

## 🐛 Проблема

Статистика показывала 0 просмотров за "Сегодня", хотя клики были.

### Причины:

1. **Формат даты (Критично!)** 💥
   - SQLite хранит дату как: `"2025-11-18 19:54:23"` (через пробел)
   - Python отправлял: `"2025-11-18T09:00:00"` (через 'T')
   - При сравнении строк: `' '` (пробел) < `'T'`
   - **Результат:** Строка из базы всегда считалась "меньше" (старее), чем пороговое значение. Клики отбрасывались.

2. **Часовой пояс (Важно)** 🕒
   - SQLite хранит время в UTC.
   - Python использовал локальное время.
   - Это давало сдвиг на 3 часа, но само по себе не объясняло полную пропажу кликов.

## ✅ Решение

В файле `src/database.py`:

1. Заменено `datetime.now()` на `datetime.utcnow()` (для UTC).
2. Заменено `.isoformat()` на `.strftime("%Y-%m-%d %H:%M:%S")` (чтобы убрать 'T').

### Было:
```python
yesterday = datetime.now() - timedelta(days=1)
# ...
(yesterday.isoformat(), ...) 
# Отправляло: "2025-11-18T09:00:00.123456"
```

### Стало:
```python
yesterday = datetime.utcnow() - timedelta(days=1)
# ...
(yesterday.strftime("%Y-%m-%d %H:%M:%S"), ...)
# Отправляет: "2025-11-18 06:00:00"
```

Теперь формат идентичен базе данных, и сравнение работает корректно!
